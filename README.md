# 🧪 TP9 — Transactions, verrouillage & sécurité (MySQL)

## 🎯 Objectif
Ce TP vise à :
- mettre en œuvre les **transactions** (`START TRANSACTION`, `COMMIT`, `ROLLBACK`)
- comprendre les **verrous** (locks) et les **niveaux d’isolation**
- sécuriser l’accès via la création d’**utilisateurs** et l’attribution de **privilèges**

---

## ✅ Prérequis
- MySQL Server 8.x (ou MariaDB)
- MySQL Workbench (optionnel) ou CLI
- Accès au terminal **CMD**
- Port (exemple) : `3307` (adapter si besoin)
